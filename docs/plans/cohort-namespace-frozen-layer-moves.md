# Cohort-file namespace moves — held-back frozen layer

The bulk ingest/MEA modules are migrated to `alz/cohorts/*`. Remaining: complete the Phase-4
migration for the paths held back to keep the frozen Incytr/decomposition layer out of a path-only
move — R extractors, `*_decompose.py`, `build_5xfad_*`, Song `song.py` + `alz/decomposition_mea/`,
`lucie.py`.

Deferred out of the cohort-namespace refactor.
