# Workflow diagrams for methods docs

Pipeline-overview terminology is rewritten and relocated to `docs/methods/`. Remaining: add
workflow diagrams (flowcharts) for both the AD and T-cell pipelines — a required deliverable for
collaborators and publications, not a polish item.
