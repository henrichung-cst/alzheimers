# Incytr audit ledger

Schema per `docs/integrations/incytr_audit_plan.md` §4.4. One row per
introducing commit; rows are merged or split during the audit when changes
span buckets.

Columns: `id` | `repo` | `bucket` | `introducing_commits` | `description` | `location` | `native_counterpart` | `equivalence_test` | `justification` | `disposition` | `parking_tag` | `verdict_date` | `signoff`

**Bucket legend**: A = factorial extension (Section A); B = performance (Section B); C = discretionary (Section C); TBD = not yet assigned.

**Disposition legend**: `signed-off` (Sprint 1 verdict); `→ Sprint N` (Sprint 1 routes the row to a later sprint for verdict); `split` (commit bundles multiple buckets, see split-row).

| id | repo | bucket | introducing_commits | description | location | native_counterpart | equivalence_test | justification | disposition | parking_tag | verdict_date | signoff |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `INC-1` | incytr | A | `44d3008` | 2026-04-15 — simplify: address code review findings | `R/factorial.R`, `R/evaluation.R`, factorial tests | n/a (touches factorial-only code) | factorial testthat suite | Cleanup of factorial scaffolding only; no semantics change. | signed-off | | 2026-05-05 | hchung |
| `INC-2` | incytr | A | `e09105a` | 2026-04-15 — phase 3: generalize TPDS/PDS pipeline for factorial mode | `R/evaluation.R` (`Pathway_evaluation`/`Cal_PDS`/`Export_results` factorial branches), `R/factorial.R`, `R/analysis.R` | native `Cal_PDS`/`Pathway_evaluation`/`Export_results` (legacy branch preserved) | `test-Cal_PDS_factorial.R`, `test-Pathway_evaluation_factorial.R`, `test-Cal_scFC_factorial.R`, `test-Export_results_factorial.R` | Adds factorial dispatch (`if (mode == "factorial")`); legacy two-condition path bitwise-identical for `sigprob`/`sc_FC`/`p_value`/`pr_FC`/`ps_FC`/`py_FC` per Sprint 0 pre-diff. PDS drift on legacy path is **not** introduced here (see INC-25). | signed-off | | 2026-05-05 | hchung |
| `INC-3` | incytr | A | `9d888ea` | 2026-04-15 — simplify: address code review findings | `R/factorial.R`, `R/analysis.R`, factorial tests | n/a | factorial testthat suite | Factorial-only cleanup; reduces test scaffolding repetition. | signed-off | | 2026-05-05 | hchung |
| `INC-4` | incytr | A | `388b6b7` | 2026-04-15 — update NAMESPACE with factorial mode exports | `NAMESPACE` | n/a | n/a | Exports factorial-mode functions. | signed-off | | 2026-05-05 | hchung |
| `INC-5` | incytr | A | `ff123e0` | 2026-04-15 — phase 2.5: wire factorial pipeline and add integration tests | `R/factorial.R::run_factorial`, `tests/testthat/helper-factorial.R`, `test-factorial_integration.R` | n/a (orchestrator is factorial-only) | `test-factorial_integration.R` | Top-level factorial driver; calls per-animal SigProb → Contrast_SigProb → factorial PDS. Legacy path untouched. | signed-off | | 2026-05-05 | hchung |
| `INC-6` | incytr | A | `ad4042e` | 2026-04-15 — phase 2.4: add Contrast_SigProb with factorial OLS and regime diagnostics | `R/factorial.R::Contrast_SigProb`, `contrast_to_conditions`, `classify_regime` | n/a (no native counterpart — native has no contrasts) | `test-Contrast_SigProb.R` (298 lines) | Core of the factorial extension; OLS over per-animal SigProb. See §7-1 design decision INC-DESIGN-1. | signed-off | | 2026-05-05 | hchung |
| `INC-7` | incytr | A | `84f258f` | 2026-04-15 — phase 2.2-2.3: add per-animal expression and SigProb computation | `R/factorial.R::Expr_bygroup_animal`, `Cal_SigProb_animal`, `compute_em_target_weight` | native `Expr_bygroup`, `Cal_SigProb` (legacy preserved) | `test-Expr_bygroup_animal.R`, `test-Cal_SigProb_animal.R` | Per-animal counterparts of native group-mean functions. Necessary for OLS in §7-1. | signed-off | | 2026-05-05 | hchung |
| `INC-8` | incytr | A | `4a05aae` | 2026-04-15 — phase 2.1: add factorial S4 slots and create_Incytr validation | `R/Incytr_class.R` (`animal_id`, `expr.byanimal`, `sigprob.byanimal`, `design`, `contrasts`) | native `Incytr` class (slots are additive, no removals) | `test-create_Incytr_factorial.R` | New slots default to NULL/empty; legacy two-condition constructor path validated unchanged. | signed-off | | 2026-05-05 | hchung |
| `INC-9` | incytr | A | `c88f479` | 2026-04-15 — simplify: address code review findings | `R/analysis.R`, `R/kinases.R`, `R/utils.R` | n/a | factorial + golden tests | Small simplification; kinases.R touch is cleanup, not scoring change (verified by diff inspection). | signed-off | | 2026-05-05 | hchung |
| `INC-10` | incytr | A | `c8e764e` | 2026-04-15 — phase 1.6: add N-condition integration tests | `tests/testthat/test-n_condition_integration.R` | n/a | itself | Tests only; no source change. | signed-off | | 2026-05-05 | hchung |
| `INC-11` | incytr | A | `bb0c991` | 2026-04-15 — phase 1.5: generalize permutation output, object_update, and Export_results | `R/analysis.R`, `R/evaluation.R`, `R/utils.R` | extends native `Permutation_test` output shape | factorial + golden tests | Generalizes shape only; legacy SigProb/p_value match per Sprint 0. | signed-off | | 2026-05-05 | hchung |
| `INC-12` | incytr | A | `d7f3254` | 2026-04-15 — phase 1.4: generalize evaluation and PDS to N conditions | `R/evaluation.R` (+13 -6) | native `Pathway_evaluation`, `Cal_PDS` | `test-Pathway_evaluation_factorial.R`, `test-Cal_PDS_factorial.R` | Small N-condition generalization; legacy two-condition collapse preserved. | signed-off | | 2026-05-05 | hchung |
| `INC-13` | incytr | A | `ca6a96e` | 2026-04-15 — phase 1.3: generalize kinase scoring to N conditions | `R/kinases.R` (+126 -139) | native `Cal_EI`, `Integr_kinasedata`, `Cal_activity_score` | `run_degenerate_2cond.sh` + 593/593 testthat | Sprint 5 verdict: **signed-off (N-condition generalization; legacy two-condition collapse preserved when `kldata` is absent).** `Cal_EI` (`R/kinases.R:569`) signature unchanged from native; body vectorized but interface identical. `Integr_kinasedata` (`R/kinases.R:639`) adds `reverse_sik_weight=0.3` and writes three slots (`kl.pathways`/`kl.evidence`/`kl.activity`) vs native's single `@kl`, but the slot proliferation is inert when `kldata = NULL`. The factorial wrapper makes zero calls to `Integr_kinasedata`, so this is observationally inert in production. The synthetic-golden `SiK_score_*` drift (~0.18) is owned by INC-28 (the augmented channel itself); INC-13 is the structural co-anchor, not the numerical driver. See `working/sprint5_added_layers_audit.md`. | signed-off | | 2026-05-05 | hchung |
| `INC-14` | incytr | A | `75bdd0d` | 2026-04-15 — phase 1.2: generalize Cal_SigProb and Cal_scFC to N conditions | `R/analysis.R` | native `Cal_SigProb`, `Cal_scFC` | `test-Cal_SigProb.R`, `test-Cal_scFC.R` | Generalizes function signatures to N conditions; legacy `sigprob` matches bitwise per Sprint 0. | signed-off | | 2026-05-05 | hchung |
| `INC-15` | incytr | A | `719c2b1` | 2026-04-15 — phase 1.1: generalize barcodes_bycondition and Expr_bygroup | `R/analysis.R`, `R/utils.R` | native helpers | `test-Expr_bygroup.R`, `test-barcodes_bycondition.R` | Per-condition vectors generalized; sigprob/FC slots match native bitwise. | signed-off | | 2026-05-05 | hchung |
| `INC-16` | incytr | A | `a2e99fd` | 2026-04-15 — simplify: address code review findings | `tests/testthat/helper-golden.R`, `tests/testthat/test-golden_output.R` | n/a | itself | Cleanup of golden test infrastructure only; this is the harness Sprint 0 extended. | signed-off | | 2026-05-05 | hchung |
| `INC-17` | incytr | A | `6da9bcc` | 2026-04-15 — phase 0.6: update S4 class slot documentation | `R/Incytr_class.R` (doc only) | n/a | n/a | Documentation only. | signed-off | | 2026-05-05 | hchung |
| `INC-18` | incytr | A | `3954fef` | 2026-04-15 — phase 0.5: standardize condition naming to use actual condition labels | `R/analysis.R`, `R/kinases.R`, `R/math.R`, `R/utils.R` | adjusts column-naming convention | golden + factorial tests | Replaces `cond1`/`cond2` with actual labels (`condA`/`condB`). Causes `EI` slot structural diff in Sprint 0 pre-diff; structural-only, not numerical. | signed-off | | 2026-05-05 | hchung |
| `INC-19` | incytr | A | `c4698ef` | 2026-04-15 — phase 0.2: generalize integrate_omics_layer parameter interface | `R/analysis.R::integrate_omics_layer` | parameter-shape generalization | `test-integrate_omics_layer.R` | Function signature widened to handle multi-condition omics; numerically equivalent in two-condition collapse. | signed-off | | 2026-05-05 | hchung |
| `INC-20` | incytr | A | `4e2e672` | 2026-04-15 — phase 0.1: refactor Integr_multiomics to list-based omics API | `R/analysis.R::Integr_multiomics` | API refactor of native `Integr_multiomics` | `test-Integr_multiomics.R` | Refactor only; Sprint 0 pre-diff confirms `pr_FC`/`ps_FC`/`py_FC` match bitwise. | signed-off | | 2026-05-05 | hchung |
| `INC-21` | incytr | A | `4a9423c` | 2026-04-15 — phase 0.4: relocate expression and EM-weight helpers | `R/analysis.R`, `R/math.R`, `R/utils.R` | code relocation only | factorial + golden tests | Pure relocation; no semantics change. | signed-off | | 2026-05-05 | hchung |
| `INC-22` | incytr | A | `ad7462b` | 2026-04-15 — phase 0.3: add tests for untested exported functions | `tests/testthat/*` | n/a | itself | Test coverage additions only. | signed-off | | 2026-05-05 | hchung |
| `INC-23` | incytr | A | `b50c2cc` | 2026-04-15 — phase 0.0: add golden output fixture and generation script | `tests/testthat/test-golden_output.R`, fixtures | n/a | itself | Test infrastructure that Sprint 0 extended. | signed-off | | 2026-05-05 | hchung |
| `INC-24` | incytr | A | `4aa30c8` | 2026-04-15 — clean up codebase: remove dead code, fix typos, extract shared helpers | `R/analysis.R`, `R/evaluation.R`, `R/kinases.R`, `R/utils.R` | n/a | factorial + golden tests | Cleanup commit; reductions in line count match dead-code removal pattern. | signed-off | | 2026-05-05 | hchung |
| `INC-25` | incytr | C | `abde752` | 2026-04-10 — add EM promiscuity weighting and edge confidence scoring to SigProb | `R/analysis.R::Cal_SigProb`, `R/factorial.R::Cal_SigProb_animal`/`run_factorial`, `R/math.R`; alzheimers-side `code/integration/wrappers/run_incytr_factorial_all_pairs.R` | native `Cal_SigProb` (no EM weight) | `run_degenerate_2cond.sh` (`sigprob` clean) + 593/593 testthat | Sprint 3 verdict: **reverted to default-off** (parking-in-place). Package defaults `em_promiscuity_weight = TRUE → FALSE` in `Cal_SigProb`, `Cal_SigProb_animal`, `run_factorial`. Wrapper-side hard-coded `em_w_vec` application gated behind `ENABLE_EM_PROMISCUITY_WEIGHT=1` env var (default off). No empirical / theoretical justification was recorded for the original default-on choice. **Re-attribution finding**: Sprint 1 named INC-25 the prime PDS-drift driver, but the synthetic Sprint 0 fixture has all `em_degree = 1` so `em_weight_log(1)=1` is a no-op; INC-25 has zero observable effect on the fixture. Residual `evaluation.PDS` drift (~0.089) flows through `kl.pathways → Cal_PDS` from INC-28 (Sprint 5 territory). On production data with non-trivial `em_degree`, the revert is substantive. See `working/sprint3_scoring_audit.md`. | signed-off (revert: default-off, parking-in-place) | | 2026-05-05 | hchung |
| `INC-26` | incytr | C | `b654fdd` | 2026-04-10 — add condition-label permutation test and optimize permutation (proposal docs) | `docs/incytr_proposals/*.md` | n/a | n/a | Sprint 5 verdict: **signed-off (docs-only).** Proposal/design docs; the implementation lands in INC-27/INC-28 and is verdicted there. See `working/sprint5_added_layers_audit.md`. | signed-off (docs-only) | | 2026-05-05 | hchung |
| `INC-27` | incytr | C | `f922c3a` | 2026-04-10 — add condition-label permutation test and optimize permutation internals | `R/Incytr_class.R`, `R/analysis.R` (`Permutation_test`, `permutation_test_condition`, `run_permutation_loop`), `NAMESPACE` | extends native `Permutation_test` (cell-identity shuffle only) | 593/593 testthat (incl. permutation tests) | Sprint 5 verdict: **signed-off (additive; default dispatch matches native).** `Permutation_test` (`R/analysis.R:667`) dispatches on `type=c("cell_identity","condition")` with `cell_identity` (native behavior) as the default. `permutation_test_condition` (`:804`) and `run_permutation_loop` (`:3`) are private helpers reachable only via `type="condition"`. Calling `Permutation_test()` with no `type` is bitwise-equivalent to native. See `working/sprint5_added_layers_audit.md`. | signed-off (additive) | | 2026-05-05 | hchung |
| `INC-28` | incytr | C | `6858063` | 2026-04-10 — memory permutation pass | `R/kinases.R` (+651), `R/Incytr_class.R` (`kl.evidence`, `kl.activity` slots), `R/evaluation.R::Cal_PDS` | extends native kinase channel (native has only `@kl`) | `run_degenerate_2cond.sh` (must-match SigProb/FC clean; SiK_score_*/PDS drift exposed only when `kldata` is non-null) + 593/593 testthat | Sprint 5 verdict: **signed-off (additive kinase channel; default-inert when `kldata` absent).** The new slots `kl.evidence` (`R/Incytr_class.R:81`) / `kl.activity` (`:82`) are written only by `Integr_kinasedata` when `kldata` is non-null. `Cal_PDS` (`R/evaluation.R:471`) reads `@kl.evidence` (`:490–512`) and `@kl.pathways` (`:545–546, 618–619`) only after the slots are populated; default constructions no-op. **Drift attribution finalized**: synthetic-golden `SiK_score_*` drift (~0.18) and residual `evaluation.PDS` drift (~0.089) are *both* owned by this row — they are the expected signature of the augmented channel on the golden, not regressions on the baseline `kldata=NULL` path. The factorial wrapper does not invoke `Integr_kinasedata`, so the drift is unobservable in production. See `working/sprint5_added_layers_audit.md`. | signed-off (additive; default-inert) | | 2026-05-05 | hchung |
| `INC-29` | incytr | C | `d6d5c8c` | 2026-03-27 — kinase library adaptor | `R/kinases.R::as_kldata` (`:534`), `as_kl_evidence` (`:555`), `NAMESPACE` | n/a (new external adaptor) | `test-as_kldata.R` | Sprint 5 verdict: **signed-off (adapter; no native counterpart, not on default code path).** `as_kldata` normalizes alternate column names to canonical `gene`/`site_pos`/`motif.geneName`; `as_kl_evidence` accepts data frames or named lists and outputs canonical `kinase`/`substrate`/`site_pos`/`score`/`p_value`/`padj`. Both are called only from `Integr_kinasedata` (INC-28) and end-user setup code; neither runs unless an external kinase-library object is passed in. See `working/sprint5_added_layers_audit.md`. | signed-off (adapter) | | 2026-05-05 | hchung |
| `INC-30` | incytr | C | `c3580fc` | 2026-03-27 — apply cutoff to all omics slots and fix helper return/input handling | `R/Incytr_class.R` (`Ack_FC`, `KGG_FC`, `Rme1_FC` slots), `R/analysis.R`, `R/evaluation.R`, `R/utils.R` | scope expansion: PTM tracks not in native | `run_degenerate_2cond.sh` (PTM "data not found … skipped" notes confirm slots are inert when PTM data is absent) + 593/593 testthat | Sprint 4 verdict: **signed-off as scope expansion.** New slots `Ack_FC`/`KGG_FC`/`Rme1_FC` are additive (no native counterpart) and parallel the existing serine/threonine track without perturbing the canonical PDS/TPDS pipeline. Filter portion split to INC-30.b. See `working/sprint4_filter_audit.md`. | signed-off (scope expansion) | | 2026-05-05 | hchung |
| `INC-30.b` | incytr | C | `c3580fc` (split) | filter portion of c3580fc — `apply cutoff to all omics slots` | `R/analysis.R`, `R/evaluation.R` (cutoff branches) | native `Cal_SigProb`: `cutoff_SigProb = NULL` (default-no-filter) at `R/analysis.R:606` of `93b9881` | 593/593 testthat (incl. `test-apply_path_cutoff.R`) | Sprint 4 verdict: **default-inert.** Native default is `cutoff_SigProb = NULL`; both codebases run no filter on the default code path. When a non-NULL cutoff is passed, the post-INC-30 code applies it uniformly across all omics slots (principled extension for a multi-PTM codebase, filter-consistency invariant). Cannot silently introduce a behavior change masked by tests. See `working/sprint4_filter_audit.md`. | signed-off (default-inert; opt-in cutoff applies uniformly across all omics slots when set) | | 2026-05-05 | hchung |
| `INC-31` | incytr | A | `847a014` | 2026-03-10 — docs: Add docs/notes to .gitignore | `.gitignore` | n/a | n/a | Chore; no scientific impact. | signed-off | | 2026-05-05 | hchung |
| `INC-32` | incytr | A | `5031094` | 2026-03-10 — add env.yml | `env.yml` | n/a | n/a | Chore; environment config. | signed-off | | 2026-05-05 | hchung |
| `INC-33` | incytr | A | `650bca1` | 2026-03-09 — Remove CLAUDE.md from tracking | `CLAUDE.md`, `.gitignore` | n/a | n/a | Chore. | signed-off | | 2026-05-05 | hchung |
| `INC-34` | incytr | A | `b2a4c7f` | 2026-03-09 — Organize docs into reference and notes | `docs/*` | n/a | n/a | Chore; doc reorganization. | signed-off | | 2026-05-05 | hchung |
| `INC-35` | incytr | B | `e04bf18` | 2026-03-06 — Improve evaluation/kinase code organization | `R/evaluation.R`, `R/kinases.R` | code organization (logi→math.R, vectorized score_omics_layer / Cal_PDS / Cal_EI; positional→name-based indexing) | `run_degenerate_2cond.sh` + 593/593 testthat | Pure refactor + positional-index cleanup; commit's own claim "13/13 snapshots exact match" reproduced — sigprob/sc_FC/p_value/pr_FC/ps_FC/py_FC bitwise-clean against native `93b9881`. | signed-off | | 2026-05-05 | hchung |
| `INC-36` | incytr | B | `76becbd` | 2026-03-06 — Remove dplyr dependency, replace with base R + data.table | all R files | dependency removal (30 dplyr call sites → base R + data.table) | `run_degenerate_2cond.sh` + 593/593 testthat | Pure dependency swap; commit's own claim "all 148 unit tests pass, all 13 snapshots exact match" reproduced. | signed-off | | 2026-05-05 | hchung |
| `INC-37` | incytr | B | `c1b6fb9` | 2026-03-05 — Optimize performance, fix correctness issues | `R/analysis.R` (pathway dedup vectorize, permutation densify), `R/kinases.R` (Cal_EI hoisting), `R/evaluation.R` (do.call(rbind)), `R/utils.R` (object_update FC dedup) | performance | `run_degenerate_2cond.sh` + 593/593 testthat | Performance hunks of `c1b6fb9`; numerical equivalence proven via Sprint 0 goldens. Correctness/`\|\|` fixes split out to `INC-37.b`. See `working/sprint2_perf_split.md`. | split — perf signed-off | | 2026-05-05 | hchung |
| `INC-37.b` | incytr | A | `c1b6fb9` | 2026-03-05 — correctness fixes split out from `c1b6fb9` | `R/analysis.R` `pathway_inference` null-gene fallback; `R/math.R` `setDT`→`as.data.table`; 7× `\|`→`\|\|` in scalar control flow | native `pathway_inference` (had latent bug); native `setDT` in math helpers | `run_degenerate_2cond.sh` + 593/593 testthat; per-fix verdict in `working/sprint2_perf_split.md` | All three correctness hunks are numerically neutral on the in-use code path: null-gene fallback fixes a native bug we inherit (flag for upstream PR), `setDT`→`as.data.table` removes an in-place caller mutation (no return-value change), `\|`→`\|\|` is lazy-eval defense. None route to Sprint 3/4. | split — fix-forward signed-off | | 2026-05-05 | hchung |
| `INC-38` | incytr | B | `7e6735d` | 2026-03-05 — Refactor codebase: split monolith | source split (analysis.R 1060→573 lines; math.R, utils.R extracted) | source split only | `run_degenerate_2cond.sh` + 593/593 testthat | Pure source split; commit's own claim "+23 tests (123 total), 13/13 snapshots unchanged" reproduced via Sprint 0 goldens. | signed-off | | 2026-05-05 | hchung |
| `INC-39` | incytr | A | `90c60e8` | 2026-03-05 — Reorganize project directory structure | filesystem moves | n/a | n/a | Chore. | signed-off | | 2026-05-05 | hchung |
| `INC-40` | incytr | A | `6eeeac5` | 2026-03-05 — Update documentation for development | `docs/*` | n/a | n/a | Chore. | signed-off | | 2026-05-05 | hchung |
| `INC-DESIGN-1` | incytr | A | n/a (design decision) | Per-animal SigProb vs per-condition mean SigProb | `R/factorial.R::Cal_SigProb_animal` + `Contrast_SigProb` | native: SigProb on condition-mean expression | `test-Cal_SigProb_animal.R`, `test-Contrast_SigProb.R` | Hill nonlinearity makes the two not algebraically equal. Wrapper uses (a) per-animal then OLS to get per-contrast SE/p-values. Alternative (b) condition-mean expression then OLS on SigProb was considered; rejected because it loses per-contrast SE. Documented divergence. | signed-off | | 2026-05-05 | hchung |
| `INC-DESIGN-2` | incytr | A | n/a (design decision) | `Cal_foldchange` `1e-4` correction term in factorial mode | `R/analysis.R::Cal_foldchange` and OLS callers | native: `log2(x + 1e-4)` | factorial scFC tests | Native uses an additive `1e-4` before `log2`. Factorial OLS on per-animal SigProb does not re-apply the correction (linear model on already-Hill-bounded SigProb). Verified: no second constant introduced; behavior consistent. | signed-off | | 2026-05-05 | hchung |
| `INC-DESIGN-3` | incytr | A | n/a (design decision) | `aFC` vs `log2FC` in factorial evaluation | `R/evaluation.R` factorial branch | native: `aFC` with 75th-percentile Hill cap | `test-Pathway_evaluation_factorial.R` | Factorial path computes per-contrast `aFC` analog using the same Hill-cap rule as native (verified by reading `R/evaluation.R` factorial branch). Not a `log2FC` switch. | signed-off | | 2026-05-05 | hchung |
| `INC-DESIGN-4` | alzheimers | A | n/a (design decision) | Sample filtering scope (`males-only`) | `code/integration/config_integration.py`, ALZ runners | wrapper-side filter, not Incytr modification | dual-track sensitivity analysis (`pixi run dual`) | Wrapper-level decision (avoid hormonal confounds); not a modification of Incytr semantics. Full-cohort dual-track exists for sensitivity. | signed-off | | 2026-05-05 | hchung |
| `ALZ-1` | alzheimers | n/a | `8ca0a64` | 2026-05-04 — checkpoint: subclass-attribution branch | not integration code | n/a | n/a | Branch checkpoint, unrelated to Incytr integration. | n/a | | 2026-05-05 | hchung |
| `ALZ-2` | alzheimers | C | `0adcca8` | 2026-04-28 — feat: add tyrosine phospho (pY) track | bulk pipeline + integration adapters | scope expansion (PTM track; native has no pY) | `run_degenerate_2cond.sh` ("pY fold change data not found … skipped") | Sprint 4 verdict: **signed-off as scope expansion.** Bulk-side pY parallel to the existing serine/threonine track; pairs with INC-30. Additive output column; the canonical PDS/TPDS pipeline is unchanged when pY data is absent. See `working/sprint4_filter_audit.md`. | signed-off (scope expansion) | | 2026-05-05 | hchung |
| `ALZ-3` | alzheimers | n/a | `cc10112` | 2026-04-21 — pipeline: complete Unit 6.4 retire legacy | not integration code | n/a | n/a | Bulk pipeline cleanup, out of audit scope. | n/a | | 2026-05-05 | hchung |
| `ALZ-4` | alzheimers | n/a | `b9f302b` | 2026-04-21 — pipeline: complete Unit 6.2 retire backbone | not integration code | n/a | n/a | Bulk pipeline cleanup, out of audit scope. | n/a | | 2026-05-05 | hchung |
| `ALZ-5` | alzheimers | n/a | `5bab404` | 2026-04-21 — pipeline: complete Unit 6.1 retire old viewers | viewer | n/a | n/a | Out of audit scope (viewer). | n/a | | 2026-05-05 | hchung |
| `ALZ-6` | alzheimers | n/a | `2ab47fb` | 2026-04-20 — pipeline: lossless edge sharding for unified viewer | viewer | n/a | n/a | Out of audit scope (viewer). | n/a | | 2026-05-05 | hchung |
| `ALZ-7` | alzheimers | n/a | `e096f36` | 2026-04-20 — update edges | viewer | n/a | n/a | Out of audit scope (viewer). | n/a | | 2026-05-05 | hchung |
| `ALZ-8` | alzheimers | n/a | `99ab2b3` | 2026-04-20 — pipeline: complete Unit 1.3 build_edge_index | viewer index | n/a | n/a | Out of audit scope. | n/a | | 2026-05-05 | hchung |
| `ALZ-9` | alzheimers | C | `97616a6` | 2026-04-16 — Factorial permutation tests with within-receiver shuffles | `code/integration/run_factorial_permutations.sh`; `code/integration/adapters/aggregate_factorial.py::_run_permutation_one_contrast`, `run_backbone_permutations` | added analysis layer (no native counterpart) | runner produces sidecar CSV at `intermediates/factorial/all_pairs/aggregation/backbone_permutation_pvalues_by_contrast.csv` | Sprint 5 verdict: **signed-off (separate stage; q-values written to sidecar CSV, never gate native PDS).** Runner spawns one `_run_permutation_one_contrast` invocation per contrast (9 contrasts: App/Tau/ApTt × 2mo/4mo/6mo) and concatenates per-contrast results from `perm_tmp/` into the sidecar CSV. `recv_*.parquet` is unmodified. Storey's fixed-λ pi0 estimator (`aggregate_factorial.py:499`) computes q-values; λ here is the pi0 tuning parameter, not a blending weight. Matches audit-plan §5 Sprint 5 directive verbatim. See `working/sprint5_added_layers_audit.md`. | signed-off (separate stage) | | 2026-05-05 | hchung |
| `ALZ-10` | alzheimers | n/a | `2dda892` | 2026-04-15 — brain atlas data compression | data only | n/a | n/a | Out of audit scope. | n/a | | 2026-05-05 | hchung |
| `ALZ-11` | alzheimers | C | `f6e2b98` | 2026-04-14 — Simplify permutation code after review | permutation code under `code/integration/adapters/` | added analysis layer | covered by ALZ-9's sidecar-CSV gate | Sprint 5 verdict: **signed-off (separate stage).** Cleanup of ALZ-9 helpers; identical disposition. See `working/sprint5_added_layers_audit.md`. | signed-off (separate stage) | | 2026-05-05 | hchung |
| `ALZ-12` | alzheimers | C | `9cf31de` | 2026-04-14 — Backbone-level permutation tests and per-pair | permutation code under `code/integration/adapters/` | added analysis layer | covered by ALZ-9's sidecar-CSV gate | Sprint 5 verdict: **signed-off (separate stage).** Same scope as ALZ-9; per-pair backbone permutations write to the same sidecar CSV and never modify `recv_*.parquet`. See `working/sprint5_added_layers_audit.md`. | signed-off (separate stage) | | 2026-05-05 | hchung |
| `ALZ-13` | alzheimers | B | `019334b` | 2026-04-14 — Cross-pair aggregation and Parquet-only cleanup | `code/integration/adapters/` | data path / IO refactor (no scoring change) | `run_verify_phase2.sh` (gated on Phase 1/2 fixtures) | IO/aggregation refactor; numerical claim covered by `verify_phase2.R` regression test (Phase 1 CSV vs Phase 2 Parquet at tol=1e-10). When intermediates are absent the runner emits SKIP; the suite is whatever the most recent all-pairs run produced. | signed-off | | 2026-05-05 | hchung |
| `ALZ-14` | alzheimers | A | `8cf4366` | 2026-04-14 — Update integration README for Phase 2 | `code/integration/README.md` | n/a | n/a | Documentation only. | signed-off | | 2026-05-05 | hchung |
| `ALZ-15` | alzheimers | B | `8679681` | 2026-04-14 — Vectorized receiver scoring: Phase 2 of all-pairs | `code/integration/wrappers/receiver_scoring.R` | replaces native receiver scoring with vectorized rewrite | `code/integration/tests/run_verify_phase2.sh` (D3) | Performance rewrite. `verify_phase2.R` promoted from manual ritual to one-command runner at `tol=1e-10`; runner SKIPs when Phase 1/2 intermediates are absent and asserts when they exist. Last in-tree run vs available Phase 2 Parquets matched within tolerance. | signed-off | | 2026-05-05 | hchung |
| `ALZ-16` | alzheimers | B | `291fa00` | 2026-04-14 — Receiver-centric backbone enumeration: Phase 1 | `code/integration/wrappers/run_incytr_all_pairs.R` Phase 1 receiver-centric enumeration | replaces native pathway_inference data.table joins (sender-centric) | covered together with ALZ-18 by `run_duckdb_enumeration_equiv.sh` | Phase 1 receiver-centric enumeration is the precursor to ALZ-18's DuckDB rewrite; the current pipeline runs Phase 2 (DuckDB) end-to-end, so equivalence is established via ALZ-18's bitwise pathway-set match. | signed-off | | 2026-05-05 | hchung |
| `ALZ-17` | alzheimers | B | `98d2b9d` | 2026-04-14 — remove dead code | `code/integration/` (deleted code paths) | n/a | n/a | Pure dead-code removal; no runtime behavior change. | signed-off | | 2026-05-05 | hchung |
| `ALZ-18` | alzheimers | B | `3e688bf` | 2026-04-14 — All-pairs Incytr pipeline: DuckDB enumeration | `code/integration/wrappers/duckdb_enumeration.R` | replaces native `pathway_inference` cartesian merge with DuckDB join + R-side edge pre-prune | `code/integration/tests/run_duckdb_enumeration_equiv.sh` (D2) | Bitwise-identical pathway set on synthetic 12-gene × 40-cell × 2-condition fixture vs native `pathway_inference()`, with `cutoff_SigProb = 0` and `em_promiscuity_weight = FALSE` (pre-prune cutoffs are bucket C, Sprint 4). | signed-off | | 2026-05-05 | hchung |
| `ALZ-19` | alzheimers | C | `9f339ed` | 2026-04-13 — Kinase-imputed pathway expansion | `code/integration/adapters/export_kinase_imputed_genes_factorial.py`, R floor `EXPR_IMPUTATION_FLOOR=0.05`, soft-rescue weighting at `run_incytr_factorial_all_pairs.R:471–495` | gene-admission rule change (no native counterpart) | `run_factorial_all_pairs.sh:70` shell-gates the entire imputation step on `ENABLE_KINASE_AUGMENTATION=1`; baseline `run_degenerate_2cond.sh` keeps must-match SigProb/FC slots clean | Sprint 4 verdict: **signed-off (parking-in-place behind `ENABLE_KINASE_AUGMENTATION`).** With the flag off, no `kinase_imputed_genes__*.csv` files exist and `load_imputed_for_recv_factorial(recv)` returns NULL, short-circuiting the rescue block at line 473 (`!is.null(recv_imputed_df) && nrow(recv_imputed_df) > 0`). `EXPR_IMPUTATION_FLOOR` is read unconditionally but consumed only inside that nested guard; default-run no-op. The flag is now also forwarded through `run_factorial_all_pairs.sh:85`. See `working/sprint4_filter_audit.md`. | signed-off (parking-in-place behind ENABLE_KINASE_AUGMENTATION) | | 2026-05-05 | hchung |
| `ALZ-20` | alzheimers | C | `19de928` | 2026-04-13 — Substrate-based external reranking with median | `code/integration/adapters/compute_kinase_support_factorial.py` | no native counterpart | sidecar outputs at `factorial/all_pairs/{sender}__{receiver}/kinase_support_scores.csv` (`:260`) and optional `kinase_routes.parquet` (`:278`); not invoked from `run_factorial_all_pairs.sh` | Sprint 5 verdict: **signed-off (separate downstream; reads baseline Parquet, writes sidecar CSV; never overwrites native PDS).** Reads `recv_*.parquet` (`:128`) and writes per-pair sidecar files; must be run as a separate downstream stage (mentioned only in the trailing echo summary at `run_factorial_all_pairs.sh:109`). `aggregate_factorial.py` joins the sidecar CSV via DuckDB (`:324`) for summary statistics; no merged parquet is written back. **No λ-style PDS reranking exists in the codebase** — the λ at `aggregate_factorial.py:499` is Storey's pi0 fixed-λ q-value parameter, not a blending weight. Matches audit-plan §5 Sprint 5 directive verbatim. See `working/sprint5_added_layers_audit.md`. | signed-off (separate downstream) | | 2026-05-05 | hchung |
| `ALZ-21` | alzheimers | A | `5acec95` | 2026-04-09 — Phase 1 Incytr integration: adapters, R wrappers | `code/integration/{adapters,wrappers}/` initial scaffold | initial integration framework | n/a (framework) | Initial integration scaffold; the framework itself is A-bucket. Subsequent commits introduce specific scope-expansion items (above) that are routed to later sprints. | signed-off | | 2026-05-05 | hchung |
| `ALZ-22` | alzheimers | C | wrapper config (multi-commit) | wrapper-side gene admission rule replacing native `Find_highexp_gene` | `code/integration/wrappers/run_incytr_*.R` (`EXPR_DETECTION_THRESHOLD = 0.10` env default), Python adapters that build `gene.use_Sender`/`gene.use_Receiver` whitelists | native `Find_highexp_gene` (`cutoff_percentile = 0.5`, byte-identical in package; bypassed by all-pairs wrappers) | n/a (configuration choice) | Sprint 3 verdict: **wrapper-config divergence, justified.** The all-pairs wrappers always pass explicit `gene.use_Sender`/`gene.use_Receiver` whitelists, so native `Find_highexp_gene` is never invoked. The 10% snRNA detection-rate rule is appropriate for sparse single-nucleus data (5–10× lower UMI than scRNA-seq); native's 50th-percentile-of-expressed cutoff was designed for scRNA-seq density. Package `Find_highexp_gene` is unmodified. Audit-plan §5 Sprint 3 explicit-target item resolved. | signed-off | | 2026-05-05 | hchung |
| `INC-DESIGN-5` | incytr | A | n/a (design verification) | `logi()` k parameter audit | `R/math.R::logi`, `R/evaluation.R` (`k_logi=2`), `R/analysis.R::Cal_PDS_with_Cal_TPDS` (`k_logi=2`), `R/factorial.R` (`k_logi=2/log(2)`) | native `logi(x,k)=2/(1+exp(-k*x))-1` (byte-identical) | `run_degenerate_2cond.sh` (sc_FC/p_value/pr_FC/ps_FC/py_FC clean) | Sprint 3 verdict: **no drift.** `logi()` body byte-identical to native. Legacy two-condition path uses `k_logi = 2` (native default). Factorial path uses `k_logi = 2/log(2)`, documented as a deliberate scale calibration so the logistic operates on log-FC inputs at the same effective steepness as native `logi(aFC, k=2)`. Verified via `test-Pathway_evaluation_factorial.R`. Pairs with INC-DESIGN-3. | signed-off | | 2026-05-05 | hchung |
| `ALZ-18.b` | alzheimers | C | `3e688bf` (split) | filter portion of ALZ-18 — DuckDB pre-prune `cutoff_SigProb = 0.01` | `code/integration/wrappers/duckdb_enumeration.R:67,150–163,294–295`; centralised assignment at `code/integration/wrappers/run_incytr_factorial_all_pairs.R:86` (Sprint 4: now `Sys.getenv("DUCKDB_CUTOFF_SIGPROB", "0.01")`) | native `Cal_SigProb`: `cutoff_SigProb = NULL` at `93b9881:R/analysis.R:606` | `run_duckdb_enumeration_equiv.sh` (D2) — bitwise pathway-set match against native `pathway_inference()` with `DUCKDB_CUTOFF_SIGPROB=0` | Sprint 4 verdict: **keep-flagged (parking-in-place via `DUCKDB_CUTOFF_SIGPROB`).** The OR-test `(h1 >= cutoff)\|(h2 >= cutoff)` pre-prune at 0.01 is mathematically lossless for any reasonable top-K cut: SigProb is a multiplicand in PDS, and pathways below 0.01 in BOTH conditions cannot contribute non-negligible TPDS. Setting `DUCKDB_CUTOFF_SIGPROB=0` opts back into native-equivalent enumeration; the existing Sprint-2 equivalence runner exercises that path on every CI invocation. The flag is forwarded through `run_factorial_all_pairs.sh:85`. See `working/sprint4_filter_audit.md`. | signed-off (keep-flagged: DUCKDB_CUTOFF_SIGPROB env override) | | 2026-05-05 | hchung |
| `ALZ-23` | alzheimers | C | `5acec95` + adapter follow-ups | `ENABLE_CELLTYPE_MAPPING` WMB↔SEA-AD subclass remap | `code/integration/adapters/export_multiomics_evidence_factorial.py:42` | n/a (no native counterpart; identity mapping when off) | `run_degenerate_2cond.sh` (must-match clean with flag unset) | Sprint 4 verdict: **signed-off (parking-in-place behind `ENABLE_CELLTYPE_MAPPING`).** Default branch returns identity mapping `{ct: ct for ct in cell_types}` with strategy `"exact_or_global"`; omics columns are emitted keyed on the WMB-class taxonomy already produced by `expression_metadata.csv`. The legacy SEA-AD-to-WMB compatibility map at `SEAAD_TO_WMB_CLASS_CSV` is loaded only when `ENABLE_CELLTYPE_MAPPING=1`. The flag is now also forwarded through `run_factorial_all_pairs.sh:85`. See `working/sprint4_filter_audit.md`. | signed-off (parking-in-place behind ENABLE_CELLTYPE_MAPPING) | | 2026-05-05 | hchung |
| `INC-DESIGN-6` | alzheimers | A | n/a (design verdict) | Native SiK channel currently absent from factorial wrapper | `code/integration/wrappers/run_incytr_factorial_all_pairs.R` (no calls to `Integr_kinasedata` / `Cal_PDS` / `kl.pathways` / `as_kldata` / `as_kl_evidence` / `Permutation_test`) | native `Cal_EI` → `Integr_kinasedata` → `kl.pathways` → `Cal_PDS` chain | n/a (deferred to follow-up PR) | Sprint 5 verdict: **design-decision row — native SiK is *absent*, not gated off.** Audit-plan §5 cross-cutting concern called for splitting `ENABLE_KINASE_AUGMENTATION` so native SiK runs by default and only the external kinase support score is opt-in. Discovery: there is no `ENABLE_KINASE_AUGMENTATION` flag in the code at all (the actual flag is `ENABLE_KINASE_IMPUTATION` at `run_factorial_all_pairs.sh:76`, gating only the kinase-imputed gene expansion adapter), and the factorial wrapper never invokes `Integr_kinasedata`. The wrapper header explicitly states "Phospho, kinase scoring, and downstream reranking are deferred to a second PR." Wiring native SiK requires substantive code work (passing `kldata`/`kl_evidence` through the wrapper, choosing the contrast feeding the kinase channel, handling the OLS-vs-mean SigProb interaction described in INC-DESIGN-1). **Disposition: deferred to follow-up PR.** Suggested post-audit flag scheme: `ENABLE_NATIVE_SIK=1` (default ON once wired), `ENABLE_KINASE_SUPPORT_SCORE=0` (default OFF). Closing the audit on the current state is in scope for Sprint 5; adding the wiring is feature work, not audit cleanup. See `working/sprint5_added_layers_audit.md`. | signed-off (deferred to follow-up PR) | | 2026-05-05 | hchung |

## Sprint 1 verdict summary

- **27 A-bucket rows signed off** (INC-1..12, INC-14..24, INC-31..34, INC-39..40, ALZ-14, ALZ-21, INC-DESIGN-1..3) + ALZ-DESIGN-4 = 28.
- **1 row routed to Sprint 5 with A-bucket co-suspect status**: INC-13 (`ca6a96e` phase 1.3 generalize kinase scoring) — co-suspect for SiK drift.
- **2 rows routed to Sprint 3**: INC-25.
- **9 rows routed to Sprint 5**: INC-26, INC-27, INC-28, INC-29, ALZ-9, ALZ-11, ALZ-12, ALZ-20.
- **3 rows routed to Sprint 4**: INC-30 (split: INC-30 + INC-30.b), ALZ-2, ALZ-19.
- **6 rows routed to Sprint 2**: INC-35, INC-36, INC-37 (split-pending), INC-38, ALZ-13, ALZ-15, ALZ-16, ALZ-17, ALZ-18 (= 9 actually, reflecting ALZ refactor commits).
- **8 rows marked `n/a` (out of audit scope)**: ALZ-1, ALZ-3..8, ALZ-10.
- **4 design-decision rows recorded**: INC-DESIGN-1..4.

The two PDS / SiK_score drift candidate-leads from the Sprint 0 pre-diff are now attributed:
- PDS drift → INC-25 (`abde752`) → Sprint 3.
- SiK_score drift → INC-28 (`6858063`) primary, INC-13 (`ca6a96e`) co-suspect → Sprint 5.

## Sprint 2 verdict summary

- **9 B-bucket rows signed off**: INC-35, INC-36, INC-37, INC-38, ALZ-13, ALZ-15, ALZ-16, ALZ-17, ALZ-18.
- **1 split-row added**: INC-37.b (correctness fixes split out of `c1b6fb9`); all three correctness hunks are numerically neutral on the in-use code path, so no re-routing to Sprint 3/4. The null-gene fallback fix and `setDT`→`as.data.table` fix are both **fix-forward** (native `93b9881` carries the same latent bugs); flagged for a follow-up upstream PR.
- **No Sprint-2 rows routed onward**.

Sprint 2 verification gates (all green on 2026-05-05):
- `code/integration/tests/run_degenerate_2cond.sh` — Sprint-1 anchor; must-match SigProb/FC slots remain bitwise-clean.
- `code/integration/tests/run_duckdb_enumeration_equiv.sh` (new, D2) — DuckDB pathway set bitwise-identical to native `pathway_inference()` on the synthetic fixture with cutoffs disabled.
- `code/integration/tests/run_verify_phase2.sh` (new, D3) — promotes `verify_phase2.R` from manual ritual to one-command runner at `tol=1e-10`; SKIPs when Phase 1/2 intermediates are absent.
- `Rscript -e 'pkgload::load_all("../incytr"); testthat::test_dir(...)'` — 593/593 passing across 216 files.

Sprint 2 introduced no code changes to the `incytr` package (read-only sprint).
Wrapper-side artifacts: two new test runners under `code/integration/tests/`,
plus the working note `docs/integrations/working/sprint2_perf_split.md`.

## Sprint 3 verdict summary

- **1 C-bucket row reverted**: INC-25 (`abde752` EM promiscuity weight) — package defaults flipped `TRUE → FALSE` in `Cal_SigProb`, `Cal_SigProb_animal`, `run_factorial`; wrapper hard-coded application gated behind `ENABLE_EM_PROMISCUITY_WEIGHT=1` (default off). Parking-in-place per the `ENABLE_KINASE_AUGMENTATION` precedent.
- **2 new ledger rows**: ALZ-22 (wrapper-side gene admission, snRNA detection threshold — signed off as wrapper-config divergence with snRNA-seq justification); INC-DESIGN-5 (`logi()` k parameter audit — no drift, signed off).
- **Re-attribution finding**: Sprint 1 named INC-25 the prime PDS-drift driver, but Sprint 0's synthetic fixture has all `em_degree = 1` so the EM weight is a no-op; the residual ~0.089 PDS drift flows through `kl.pathways → Cal_PDS` from INC-28 (Sprint 5). The INC-25 revert remains substantive on production data where `em_degree` is non-trivial.

Sprint 3 verification gates (all green on 2026-05-05):
- `code/integration/tests/run_degenerate_2cond.sh` — must-match SigProb/FC slots remain bitwise-clean against native; the `evaluation.PDS` slot remains a deferred drift owned by Sprint 5.
- `code/integration/tests/run_duckdb_enumeration_equiv.sh` — DuckDB enumerator still bitwise-equivalent to native `pathway_inference()`.
- `code/integration/tests/run_verify_phase2.sh` — SKIPs gracefully (Phase 1 fixtures absent).
- `Rscript -e 'pkgload::load_all("../incytr"); testthat::test_dir(...)'` — 593/593 passing.

Sprint 3 introduced edits to the `incytr` package: 3 default-flip lines plus
docstring updates in `R/analysis.R` and `R/factorial.R`. Wrapper-side: env-var
gate around the EM-weight application in `code/integration/wrappers/run_incytr_factorial_all_pairs.R`. Working note: `docs/integrations/working/sprint3_scoring_audit.md`.

## Sprint 4 verdict summary

- **6 C-bucket filter rows resolved**:
  - `ALZ-18.b` (new split row from ALZ-18) — DuckDB pre-prune `cutoff_SigProb = 0.01` kept-flagged with `DUCKDB_CUTOFF_SIGPROB` env-var opt-out (set `=0` for native-equivalent enumeration). Justified by the mathematical argument that pathways below 0.01 SigProb in BOTH conditions cannot contribute non-negligible TPDS.
  - `ALZ-19` (kinase-imputed receiver expansion) and `EXPR_IMPUTATION_FLOOR` — signed off as already parked behind default-off `ENABLE_KINASE_AUGMENTATION`. Default-run no-op confirmed by reading the shell + R guards.
  - `INC-30` (PTM scope expansion: `Ack_FC`/`KGG_FC`/`Rme1_FC` slots) — signed off as additive scope expansion with no native counterpart; degenerate-2cond runner shows the slots emit "data not found … skipped" notes when PTM data is absent.
  - `INC-30.b` (cutoff-applied-to-all-omics-slots, split from `c3580fc`) — signed off as default-inert; native `Cal_SigProb` has `cutoff_SigProb = NULL` default, so the filter doesn't run on the default code path.
  - `ALZ-2` (bulk-side pY track) — signed off as scope expansion parallel to INC-30.
  - `ALZ-23` (new row, `ENABLE_CELLTYPE_MAPPING` WMB↔SEA-AD remap) — signed off as parked behind default-off flag; default branch returns identity mapping.
- **Wrapper edits (1 file)**: `run_incytr_factorial_all_pairs.R:86` replaces the hard-coded `cutoff_SigProb <- 0.01` with `Sys.getenv("DUCKDB_CUTOFF_SIGPROB", "0.01")`; `run_factorial_all_pairs.sh:85` forwards `ENABLE_EM_PROMISCUITY_WEIGHT`, `ENABLE_CELLTYPE_MAPPING`, and `DUCKDB_CUTOFF_SIGPROB` through systemd-run env-isolation, with the env-var section of the script header updated to document them.
- **No `incytr` package edits** (read-only sprint).

Sprint 4 verification gates (all green on 2026-05-05):
- `code/integration/tests/run_degenerate_2cond.sh` — must-match SigProb/FC slots remain bitwise-clean against native; PTM "data not found … skipped" notes confirm INC-30/ALZ-2 are inert when PTM data is absent.
- `code/integration/tests/run_duckdb_enumeration_equiv.sh` — DuckDB enumeration with `cutoff_SigProb = 0` produces bitwise-identical pathway set to native `pathway_inference()`, validating the `DUCKDB_CUTOFF_SIGPROB=0` opt-out.
- `code/integration/tests/run_verify_phase2.sh` — SKIPs gracefully (Phase 1 fixtures absent).
- `Rscript -e 'pkgload::load_all("../incytr"); testthat::test_dir(...)'` — 593/593 passing.

Working note: `docs/integrations/working/sprint4_filter_audit.md`.

## Sprint 5 verdict summary

- **9 C-bucket added-analysis-layer rows resolved**:
  - `INC-13` — N-condition kinase scoring generalization signed off; legacy two-condition collapse preserved when `kldata=NULL`. Structural co-anchor for the synthetic-golden `SiK_score_*` drift, but not the numerical driver.
  - `INC-26` — proposal docs only, signed off.
  - `INC-27` — condition-label permutation test signed off as additive; `Permutation_test()` with no `type` argument is bitwise-equivalent to native cell-identity shuffle.
  - `INC-28` — memory permutation pass + `kl.evidence`/`kl.activity` slots signed off as additive kinase channel; default-inert when `kldata=NULL`. **Drift attribution finalized**: both the synthetic-golden `SiK_score_*` drift (~0.18) and the residual `evaluation.PDS` drift (~0.089) are owned by this row — they are the expected signature of the augmented channel on the golden, not regressions on the baseline `kldata=NULL` path. The factorial wrapper does not invoke `Integr_kinasedata`, so the drift is unobservable in production.
  - `INC-29` — `as_kldata`/`as_kl_evidence` adapters signed off; not on default code path.
  - `ALZ-9`/`ALZ-11`/`ALZ-12` — backbone permutations signed off as separate stage; sidecar CSV at `aggregation/backbone_permutation_pvalues_by_contrast.csv`; never modifies `recv_*.parquet`.
  - `ALZ-20` — `compute_kinase_support_factorial.py` signed off as separate downstream; sidecar CSV/parquet under `factorial/all_pairs/{sender}__{receiver}/`; not invoked from `run_factorial_all_pairs.sh`. **No λ-style PDS reranking exists in the codebase**; the λ at `aggregate_factorial.py:499` is Storey's pi0 fixed-λ q-value parameter.
- **1 new design-decision row added**: `INC-DESIGN-6` — native SiK channel is *absent* from the factorial wrapper, not gated off. The audit-plan's `ENABLE_KINASE_AUGMENTATION` flag does not exist in the code (the actual flag is `ENABLE_KINASE_IMPUTATION`, gating only kinase-imputed gene expansion). Wiring native SiK is feature work and is deferred to a follow-up PR; the audit closes on the current state.
- **Flag-name correction**: Sprint 4 ledger rows for `ALZ-19` and `INC-25` reference `ENABLE_KINASE_AUGMENTATION`, but the actual flag is `ENABLE_KINASE_IMPUTATION` (`run_factorial_all_pairs.sh:76`). Sprint-4-as-shipped is left immutable; this Sprint 5 summary records the corrected name. The Sprint 4 verdicts are unaffected (the gating logic is correct; only the flag *name* in the ledger is wrong).
- **No `incytr` package edits and no `alzheimers` code edits** (read-only sprint, like Sprints 2 and 4). Sprint 5 is documentation-only.

Sprint 5 verification gates (all green on 2026-05-05):
- `code/integration/tests/run_degenerate_2cond.sh` — must-match SigProb/FC slots remain bitwise-clean against native; `evaluation.PDS` and `SiK_score_*` drift remain owned by INC-28 (additive kinase channel; not a regression on the baseline `kldata=NULL` path).
- `code/integration/tests/run_duckdb_enumeration_equiv.sh` — DuckDB enumeration still bitwise-equivalent to native `pathway_inference()`.
- `code/integration/tests/run_verify_phase2.sh` — SKIPs gracefully (Phase 1 fixtures absent).
- `Rscript -e 'pkgload::load_all("../incytr"); testthat::test_dir(...)'` — 593/593 passing across 216 files.

Working note: `docs/integrations/working/sprint5_added_layers_audit.md`.

## Audit close (2026-05-05)

All five sprints are signed off. The Incytr audit closes with:

- The factorial baseline output (`recv_*.parquet`) contains only native-equivalent columns; non-native columns (kinase support scores, backbone permutation q-values) live in adjunct files / downstream stages.
- Every change since fork is recorded in this ledger with a final verdict.
- Three opt-in flags expose discretionary behavior (default OFF in each case): `ENABLE_EM_PROMISCUITY_WEIGHT` (Sprint 3), `ENABLE_CELLTYPE_MAPPING` (Sprint 4), `DUCKDB_CUTOFF_SIGPROB=0` opt-out for native-equivalent enumeration (Sprint 4).
- One existing flag (`ENABLE_KINASE_IMPUTATION`, default ON) gates the kinase-imputed gene expansion adapter; it is parked-in-place per the audit's parking-in-place precedent.
- Two open follow-ups outside the audit:
  1. **Native SiK wiring** (`INC-DESIGN-6`) — separate PR to thread `Integr_kinasedata(kldata)` through the factorial wrapper, with the OLS-vs-mean SigProb design choices from `INC-DESIGN-1`. Suggested flag scheme: `ENABLE_NATIVE_SIK=1` (default ON once wired) + `ENABLE_KINASE_SUPPORT_SCORE=0` (default OFF).
  2. **Upstream PR for `INC-37.b` correctness fixes** (carried forward from Sprint 2): null-gene fallback in `pathway_inference`, `setDT`→`as.data.table` in math helpers.
