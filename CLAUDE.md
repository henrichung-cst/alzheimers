# CLAUDE.md

Project context, commands, architecture, and outputs: see [`README.md`](README.md) and [`docs/foundation/`](docs/foundation/). This file holds agent-specific operating overrides, correctness invariants, and traps.

## Project-specific behavior overrides

- **No unit test suite.** The global "run tests after impl phase" rule is satisfied here by `python alz/bulk_mea/summary.py` and the verification harness (`alz/decomposition_mea/verify_decomposition.py` — runs all four checks by default; subset with `--checks`).
- **DuckDB spill directory** is `~/.cache/duckdb` via `.envrc` to avoid OOM on tmpfs `/tmp`. If DuckDB hits disk-full, verify with `echo $DUCKDB_TEMP_DIR`.

## Drive naming — do not rename either side

Drive-side is `alzheimers_hc/`, local is `alzheimers/`. High-priority collaborators have links to `alzheimers_hc` on the drive; renaming either side breaks their access. Under `~/mnt/gdrive/` the directory appears as `alzheimers_hc`; the local project root and pixi env are `alzheimers`. Scripts mapping local → drive must not assume identity.

## Closed paths — do not reopen

(See `docs/foundation/analysis_charter.md` and the global Anti-shim rule.)

- **Closed:** direct (statistical) deconvolution, per-cluster stoichiometry, factor model, two-compartment, transcript-only rescue, factorial Incytr (archived 2026-05-18), WMB-34 spine, Levy-19 spine.
- **Active:** stoichiometry + MEA on bulk; levy_t5 forward projection (`P_c = f_c × bulk`); pair-mode Incytr on the 31-cluster spine.

Do not reintroduce closed paths as flags, fallbacks, or sensitivity toggles.

## Pair-mode Incytr correctness invariants

- Production entry: `Incytr::Cal_pairwise_grid` (`~/Projects/work/incytr/R/grid.R`). Factorial APIs `construct_factorial_paths` / `score_factorial_paths` were deleted at commit `424119f` — do not reintroduce them.
- Output shape: **31² = 961 sender × receiver pairs per contrast, 9 contrasts.**
- **Pair pvalue is untrustworthy — filter/rank pathways on `|PDS|`.**
- Integration config lives in `alz/integration/config_integration.py`, not `alz/shared/config.py`.
- Required R deps: `Incytr`, `DBI`, `duckdb`, `data.table`, `arrow`.

## Pair-mode Incytr — sce4 parity constants (do not revert)

Six call-site overrides in `alz/incytr_pair/incytr_commandline.R` and `alz/incytr_pair/build_input_gene_list.R` are required to reproduce sce4's `Top300_table` reference (599/600 Microglia → Cholinergic.Neurons — App-ligand paths now enumerate via the transgene force-include, leaving only the Depdc5 knife-edge miss; 599/600 Ndnf×Ndnf; max `|Δ sclog2FC|` = 0 on R/T). Reverting any one of them causes the regression check `pixi run verify-incytr-sce4` to fail. Investigation history: `bench/bench.md`. Propagation record: `docs/plans/sce4_fix_propagation_2026-05-23.md`.

**Method/application separation (2026-05-29).** Three method computations that were reimplemented inline in `incytr_pair` now live in the `Incytr` package and are *called* from the app — the app supplies only AD data + parameters: HEG (`Incytr::Find_highexp_gene_batch`, global cutoff scope), per-cluster prG (`Incytr::proteomics_gene(strict=TRUE)`), and the pair-invariant trimean substrate (`Incytr::precompute_expr_bygroup`, which also retired the app's `Incytr:::` internal reach). The swap is byte-identical (verified: prG setequal across all 31 clusters, HEG `max|Δ ave.exp|=0`, expr substrate `identical`), so every sce4 invariant above is preserved. Do NOT re-inline these into `incytr_pair`. Audit: `docs/plans/incytr_pair_reorg_2026-05-29.md`.

**Receiver gene.use (A33, corrected by A36; prG style → aFC at A6j 2026-05-30; per-contrast DEG 2026-05-31; HEG dropped + DEG cutoff →1 2026-05-31):** `dg_by_cluster = DEG ∪ prG` per cluster (NO HEG — see parity constant #1), where DEG is **per-contrast** (this contrast's two conditions' markers, `avg_log2FC > 1 & p_val < 1e-4`) and `prG = genes with |aFC| > 1`. `aFC = pr_log2FC · min(2·Vmax²/(Vmax²+a²), 1)` (a = pr.q quantile) is the paper/package-default `proteomics_gene` metric, and **`pr_log2FC` is computed the way the Incytr scorer computes it** — `limma::normalizeBetweenArrays()` (quantile normalization across all genes) on the floored `[cond1, cond2]` pr columns, *then* `log2`. It is **NOT** the raw column ratio. Computing prG on the raw ratio (the pre-A36 bug) drops exactly the genes the scorer flags as pr-significant: e.g. Acvr1/Cholinergic raw ratio = 0.846 (excluded) but quantile-normalized = 2.04 ≈ sce4's reference 1.997 (included). This was the cause of the long-standing "Acvr1 4× gap" — not clustering, not bulk, not a missing file. The prG selection is `Incytr::proteomics_gene(style="aFC", cutoff=1, strict=TRUE)` on the floored pr tables (the driver supplies the floor + the strict `> 1` cutoff); see the `prg_by_cluster` block in `incytr_commandline.R`. **aFC vs log2FC:** aFC's magnitude term saturates to 1 for genes with Vmax ≫ pr.q (so aFC ≈ log2FC for most prG genes); narrowing is modest (Cholinergic prG 2109→1965 ≈ 7%, Micro→Cholin paths 30067→19758 ≈ 1.5×), NOT the 30× once mis-claimed. (After the 2026-05-31 HEG drop the Micro→Cholin enumeration falls to 9,026 ≈ 7× sce4's 1,283. **This residual is NOT exonerated and the top-300 cap does NOT absorb it** — corrected later 2026-05-31, see `docs/plans/sce4_reproduction.md` §6.6. It is **sender-ligand** breadth, not prG: our Microglia emits 8 ligands vs sce4's 4, adding Ctsd/Adam17/Entpd1 (strong Microglia DEGs in sce4's OWN markers, yet absent from sce4's entire 182-ligand universe). `Ctsd→App` alone = 5,226 spurious high-|PDS| paths that **win the per-pair top-300 cap and evict real sce4 paths** → the shipped cap overlaps sce4's Top300 by only 53/600 Micro→Cholin / 276/600 Ndnf. The narrowing rule separating C1qa-kept from Ctsd-dropped is not determinable from on-disk artifacts. `verify-incytr-sce4` now gates on shipped-cap fidelity (`--min-cap-frac`) so this no longer reports false-green; recall 599/600 is enumeration-only.) Its one benchmark cost is **Depdc5** (Target of `C1qa|Cr1l|Cbfb|Depdc5`): aFC=0.9974 vs sce4's recorded 1.000444 — a `|aFC|=1` knife-edge artifact, NOT input provenance. Proven 2026-05-30: scoring prG off sce4's OWN frozen both-sex pr still gives aFC=0.99757 (≈ our males-only 0.99743); the raw Depdc5 pr matches sce4 to 4 decimals and the whole gap is in the `normalizeBetweenArrays` quantile-norm gene universe (ours qn_log2FC 1.4869 vs sce4's recorded 1.4932), so no input swap closes it (`bench/perf/test_parity_levers.sh`). The `verify-incytr-sce4` recall floor is **599** (Depdc5 is the only remaining miss after the transgene force-include). Full mechanism + lever tests: `docs/plans/sce4_reproduction.md` (§4, §7).

**The decomposition that feeds the Incytr inputs uses the PROVENANCE deconvolution, not the levy_t5 forward-projection share.** `alz/incytr_pair/export_decomposition_for_pair.py` runs `P_c = (N_total/N_c) × bulk × (specific_c / Σ_46 specific)` with min/10000 imputation: transcript share from the frozen `aggexp.csv` (the one unrecoverable upstream step — it is `AggregateExpression(slot="data")`, an SCT/model normalization baked into a Seurat object not on this box; the h5ad `.X` cannot reproduce it, raw counts give 50.0 and CPM gives 305.9 vs aggexp's 350.3), cell-count size factors regenerated byte-exactly from the Song h5ad, bulk from the frozen per-group medians (`pr/imac/py_median.csv`). This **replaces** the prior levy_t5 `log2(CPM+1)×bulk` share for the Incytr inputs (the levy_t5 `f_c×bulk` forward projection still serves the MEA decomposition — different consumer). With this, the **derived** (production-regenerated 31-spine) inputs reproduce provenance: Acvr1/Cholinergic ma_2mo = 50.70/28.21, Microglia→Cholinergic **599/600** (App-ligand paths enumerate via transgene force-include; lone miss is the Depdc5 knife-edge), Ndnf×Ndnf **599/600**, max `|Δ sclog2FC|` = 0 on R/T. The frozen 46-cluster inputs (`bench/validate_frozen_parity.sh`) give the same. Full audit: `docs/plans/sce4_decomposition_reconciliation_2026-05-24.md`.

**App-transgene: candidacy CLOSED (2026-05-30), value residual remains.** sce4 force-includes the AD model's human transgenes (App/Psen1/Mapt) into prG for every cluster, bypassing the `|aFC|>1` rule. We now do the same — `incytr_commandline.R` defines `TRANSGENES <- intersect(c("App","Psen1","Mapt"), rownames(Data.input))` and unions it into every cluster's `prg_by_cluster` (self-gating: empty for cohorts lacking the symbols, e.g. the human T-cell cohort). Before this, App was a candidate ligand only in the 28/31 clusters where it was a transcriptomic marker — **not Microglia/Astrocytes/OPC** — so its 27 Microglia→Cholinergic ligand paths never enumerated. The force-include enumerates them all: Micro→Cholin recall **572→599** (verified `bench/perf/test_parity_levers.sh`). The **only** remaining residual is the App *value*: the 27 paths carry our flat App `Ligand_sclog2FC` (≈0.19) instead of sce4's saturated 7.65 — a transcript-provenance gap not derivable from our scoring object (`hsAPP`=0 in Microglia, endogenous `App` two-sided). `verify_sce4_parity.py` requires recall ≥ 599 (Depdc5 is the lone miss); below that floor it PASSES only if every miss is an App-ligand path (signals the force-include regressed); and it tolerates Ligand/EM `|Δ sclog2FC|` outliers only when the position gene is App (`--transgene`). Do NOT fabricate App's 7.65 value to force exact value parity.

1. **gene.use = DEG ∪ prG, NO HEG; broad markers + DEG cutoff `>1` (2026-05-31, REPLACES the prior DEG>1.5 ∪ HEG recipe).** `build_input_gene_list.R` emits `allmarkers.csv` only — one-vs-rest `FindAllMarkers(logfc.threshold = 0.1, only.pos = TRUE)`, `Type_condition` idents, run **broad** to match sce4's frozen marker table (floor `avg_log2FC ≈ 0.10`). The **driver** assembles the per-cluster gene.use **per contrast**: DEG (this contrast's two conditions' markers, `avg_log2FC > 1 & p_val < 1e-4`) ∪ prG. **No `top_n(500)` cap. No HEG.** This was reverse-engineered from sce4's own `.label` provenance columns (only `DEG`/`prG` labels appear — zero `HEG` across the ma_2mo + ma_4mo references) and the frozen broad-marker floor; it OVERTURNS the prior "missing wrapper script / undeterminable" conclusion. Both benchmarks stay **599/600** (enumeration recall) and Micro→Cholin enumeration halves (19,758→9,026). **NOTE (corrected later 2026-05-31):** 599/600 is *enumeration* recall, not the shipped deliverable — the per-pair top-300 cap that the viewer ships overlaps sce4's Top300 by only 53/600 (Micro→Cholin) because residual **sender-ligand** breadth (Ctsd/Adam17/Entpd1) floods the cap. The HEG drop here is real and correct but does NOT achieve deliverable parity. See line above + `docs/plans/sce4_reproduction.md` §6.6. **Why the prior recipe was wrong:** `FindAllMarkers(logfc.threshold=1.2)` + DEG cutoff `1.5` pre-pruned real DEGs sce4 kept (Prkca/Grm5/Robo2, FC 1.0–1.2, p≪1e-4); HEG was the leaky patch that re-admitted them along with hundreds of non-DE high-expression genes — the dominant ~15× over-emission source. `>1` reproduces sce4's DEG-label membership (80/82 of its Ndnf receiver DEG nodes vs 55/82 at `>1.5`) and is symmetric with prG's `|aFC|>1`. Do NOT reintroduce HEG or raise the DEG cutoff. Mechanism + per-contrast assembly history + the VOID rule-search: `docs/plans/sce4_reproduction.md` §6.5 / §6.2 / §6.1. **For the AD cohort this DEG∪prG derivation is now REPLACED by sce4's own frozen per-pair gene.use (see #1b below, §6.7); it remains the t-cell recipe.**
1b. **AD gene.use = sce4's own pre-cap per-pair node sets (2026-05-31, REPLACES the DEG∪prG derivation for AD).** The narrowing rule that gets sce4 from `DEG(>1)∪prG` to its 182-ligand universe is NOT reconstructable from on-disk artifacts (the CellChat ligand-category candidate was REFUTED; §6.6), but sce4's *effective* gene.use IS on disk — losslessly in its pre-cap per-pair pathway tables `sce4_DEG_PRG_Pairwise_pathway_table_10302025.rds`. `extract_sce4_geneuse.R` reconstructs, **per (sender,receiver) PAIR**, `gene.use_Sender = {Ligand}`, `gene.use_Receiver = {Receptor,EM,Target}` from that pair's rds element → `data/incytr_frozen/sce4_geneuse/<c1>_<c2>.csv`; the driver consumes it via `SCE4_GENEUSE_DIR` (set only by the AD runner; t-cells leave it unset and derive). **PER-PAIR is required, NOT per-cluster** — the engine gates all receiver positions by ONE flat `gene.use_Receiver`, so a per-cluster union (broader than any single pair) lets the engine recombine cross-pair nodes into chains sce4 never enumerated (Micro→Cholin ballooned to 2,952). Per-pair reproduces the gated Allpathway EXACTLY: Micro→Cholin 1,283=1,283 (0/0); Ndnf×Ndnf symmetric diff is 100% App/Psen1 transgene paths. This is engine-monotonic (subset ⇒ 0 extra / 0 missing), the anti-shim "genuinely different data" exemption (selector = artifact presence, not a mode flag). `verify_sce4_parity.py` now gates on **gated path-set identity vs sce4's Allpathway** (symmetric diff must be transgene-only); the top-300 **cap fidelity is informational** (PDS-ranked, carries the documented phospho + App-value residuals, so it can't match regardless of gene.use correctness). On box for 5/9 contrasts; 4 pre-cap rds (ma_2mo_ApTt, ma_2mo_Ttau, ma_4mo_ApTt, ma_4mo_Ttau) pending from the Drive. Full record: `docs/plans/sce4_reproduction.md` §6.7.
2. **`pmax(pr_*, 1)` floor** — `incytr_commandline.R` (floor_pr function) floors deconvoluted protein values < 1 to 1 (pr_yuyu carries ~1e-5 deconvolution residuals that otherwise get amplified by Cal_foldchange's correction-on-zero branch).
3. **`Expr_bygroup(mean_method = NULL)`** — `incytr_commandline.R:382` uses trimean (`NULL` ⇒ `0.25·Q1 + 0.5·Q2 + 0.25·Q3`), not arithmetic mean; the latter inflates SigProb for sparse-expression genes.
4. **`Cal_SigProb(correction = 0.01)`** — `incytr_commandline.R:389` overrides the package default.
5. **`cutoff_SigProb = 0, cutoff_PDS = 0`** — `incytr_commandline.R:213-214,388,414`; the driver emits all paths and filters downstream. The downstream half is `alz/incytr_pair/filter_significant_paths.py`, which applies **sce4's own gate** (verified byte-for-byte against sce4's `Allpathway` floors + `Top300` cap): `SigProb > 0.1 (either) AND |PDS| >= 0.2`, **THEN** per (sender, receiver) pair the **top-300 PDS up ∪ top-300 PDS down** (≤600 rows/pair). Wired into `run_pair_mode.sh` after the contrast loop, in place on `wide/`. **There is NO p_adj / FDR arm** — sce4 never ran the permutation test; none of its artifacts (420 per-pair CSVs, `Allpathway`, `Top300`, the two pairwise `.rds`) carry a `p_value`/`p_adj`/`fdr`/`qval` column (checked 2026-05-30). A p_adj gate is foreign to the reference and drops paths sce4 kept (cell-sparse pairs get NA permutation p → e.g. the **Microglia→Cholinergic-Neurons** benchmark, Cholinergic 1 cell/condition at 2mo, vanishes). The nboot=100 `p_value_*` columns stay in `wide/` as informational only. The per-pair top-300 cap is also what keeps the viewer small (≈66k rows/contrast vs ≈300k with the floors alone). Replaces the prior p_adj "paper gate" (and the older `|PDS|>=0.30` storage cut). Do NOT raise the driver cutoffs to 0.1/0.2 — the filter is downstream of the driver. **Consequence for parity:** the sce4 gate canNOT read filtered `wide/` (the cap's rank-300 tie-breaking would conflate with engine parity); `pixi run verify-incytr-sce4` runs `alz/incytr_pair/verify_incytr_sce4.sh`, which **regenerates the two reference pairs unfiltered (nboot=0)** and verifies those. `run_pair_mode.sh` runs this same gate *before* the filter, so a non-reproducing run never ships filtered output.
6. **`Cal_scFC(correction = 0.01)`** — `incytr_commandline.R:435` overrides `analysis.R:248`'s default (`1e-5`). The 9.95 log2 saturation gap between our pre-fix output and sce4 was exactly `log2(0.01 / 1e-5)`. Downstream code paths that recompute sclog2FC from the transcript substrate must use ε = 0.01 (see `alz/integration/verify_pathway_round_trip.py:94` and `alz/integration/build_normalized_substrate.py`).

Known residuals (both documented and gate-exempted above): (1) the App transgene *value* — the 27 Microglia→Cholinergic App-ligand paths and 7 Ndnf App-EM paths now enumerate (candidacy closed via the force-include) but carry our flat App `sclog2FC` (≈0.19) vs sce4's 7.65, a transcript-provenance gap; (2) Depdc5 — a `|aFC|=1` knife-edge, the lone non-enumerated benchmark path. See `bench/bench.md §App transgene residual`.

## Mass identity (decomposition verification)

`Σ_c [P_c × (N_c / N_total)] ≈ bulk`, **not** `Σ_c P_c = bulk`. The `f_c` weights are per-cell rates × N_total/N_c; literal summation overshoots.

## Schema & data conventions

- When adding provenance/metadata columns, match the existing schema's type exactly (e.g., if single-contrast uses string format for `imputed_nodes`, factorial must too).
- Aggregation queries: verify whether stats (std, consistency) should be computed over raw route-level rows or pre-aggregated sender-level values.

## Pipeline-specific gotchas

- **`analysis_mode` flow** — Kedro parameter in `conf/base/parameters.yml`, default `males_only`. `KEDRO_ENV=full_cohort` overlays the sensitivity config. Affects `enrich.py`, `attribute.py`, `mechanism.py` — **not** `normalize.py` (always uses all 72 samples). Legacy `ANALYSIS_MODE` env var was retired and is silently ignored.
- **Outlier detection requires stoichiometry** — `alz/ingest/song.py --outliers` reads `stoichiometry_matrix.csv`, so `normalize.py` must run first. Falls back to total proteome if unavailable.
- **WMB prerequisite** — the bulk pipeline needs `wmb_kinase_expression.csv` / `wmb_proteome_expression.csv`; run `run_wmb_expression.sh` (or `pixi run wmb-export`) first. `run_all.sh` auto-resolves the upstream WMB h5ad + SEA-AD downloads unless `--skip-atlas`.
- **WMB region scope** — `WMB_REGION_SCOPE` defaults to `whole_brain` (correct for the specificity score's brain-wide denominator). `cortex_hpf` is a sensitivity toggle only. Active scope is stamped to `wmb_kinase_expression.scope.json`; a scope mismatch forces recompute.
- **Atlas cache compressed** — raw h5ads under `data/external/allen_abc/` are zstd-compressed (~115 GB → ~26 GB). Decompress with `bash alz/runners/supporting/decompress_atlas_cache.sh` before re-running `wmb_expression.py`. Provenance in `data/external/allen_abc/MANIFEST.json`.
- **WMB expression memory** — `wmb_expression.py --proteome` processes 6,308 genes × 13 regions; use `skip_regional=True`, `chunk_size=2000` to stay under ~30 GB RAM.
- **Stage 6 pY track gating** — `build_celltype_decomposition.py --track py` (or `both`) requires `raw_phospho_normalized_pY.csv` from Stage 1. Smoke runner tolerates missing pY; re-run `pixi run normalize` if you need it.
- **Unified-viewer hard refresh** — `build_unified_viewer.py` inlines PAYLOAD as `<script type="application/json" id="payload-data">` into `index.html`. After `pixi run viewer`, hard-refresh (Ctrl+Shift+R / Cmd+Shift+R) or the cached HTML serves stale data. Quick check: `PAYLOAD.meta.generated_at` in DevTools.
- **API caching** — delete `data/derived/caches/` to force re-fetch from MyGene / Allen Brain Atlas.
