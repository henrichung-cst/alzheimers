[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "alzheimers"
version = "0.1.0"
description = "Stoichiometry-corrected kinase attribution pipeline for Alzheimer's phosphoproteomics."
requires-python = ">=3.11"

[tool.setuptools]
packages = ["alz"]
